import { Component } from 'preact';

import './Footer.scss';

export default class Footer extends Component {
  render() {
    return (
      <div className="footer">
        This is where the footer is going to to go
      </div>
    );
  }
}
